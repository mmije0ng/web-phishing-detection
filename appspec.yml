version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/hs-phishing
permissions:
  - object: /home/ubuntu/hs-phishing/
    owner: ubuntu
    group: ubuntu
hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 60
      runas: ubuntu